package org.betech.fitnes.presentation.onboarding.pregnancyconfirm

/**
 * Empty state — Pregnancy Confirm (Pencil pqupj) is a static reassurance + CTA screen.
 * The pregnancy hard-stop is already established upstream (PregnancyNudge YES → here);
 * this VM only owns navigation. See EvaluatePregnancyHardStopUseCase for plan-gen guard.
 */
data object PregnancyConfirmState
