package org.betech.fitnes.presentation.onboarding.pregnancynudge

/**
 * Empty state — Pregnancy Nudge (Pencil M52XdD) is a stateless prompt screen.
 * VM owns no toggles; the actual hard-stop trigger lives on the next step.
 */
data object PregnancyNudgeState
