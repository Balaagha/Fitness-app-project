package org.betech.fitnes.presentation.onboarding.todaysafeworkout

/**
 * 24 · Today's Safe Workout state (Pencil QHsnW).
 *
 * Empty state — the entire session payload (5 exercises, chips, footer copy)
 * is a curated static template baked into the UI layer. NO repository, NO AI
 * trigger (pregnancy_postpartum=true hard-stop carries from SafePlan entry).
 */
data object TodaySafeWorkoutState
