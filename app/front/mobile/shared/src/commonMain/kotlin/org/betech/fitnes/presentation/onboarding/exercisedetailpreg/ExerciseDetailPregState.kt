package org.betech.fitnes.presentation.onboarding.exercisedetailpreg

/**
 * 25 · Exercise Detail (Pregnancy) state (Pencil YZ38M).
 *
 * Empty state — exercise payload (title, stats, modification copy, notes) is a
 * curated static template baked into the UI layer. NO repository, NO AI trigger
 * (pregnancy_postpartum=true hard-stop carries from SafePlan entry).
 */
data object ExerciseDetailPregState
