---
description: List all features (active, paused, archived)
---

List all features and their state.

## Steps

1. Run:
   ```
   bash .claude/scripts/list-features.sh
   ```
2. Present the output verbatim — do NOT re-format or add commentary.
3. If the user asks a follow-up ("resume X", "what is Y about"), act on it
   in a separate turn — this command is read-only by default.

## Rules

- Read-only; no edits, no pointer changes
